/*
 * Viewer Messages
 *
 * This contains all the text for the Viewer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Viewer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Viewer container!',
  },
});
