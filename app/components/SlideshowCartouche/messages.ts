/*
 * SlideshowCartouche Messages
 *
 * This contains all the text for the SlideshowCartouche component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SlideshowCartouche';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SlideshowCartouche component!',
  },
});
