/*
 * DownloadModalHelp Messages
 *
 * This contains all the text for the DownloadModalHelp component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DownloadModalHelp';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Download help',
  },
});
